{
    "build": {
        "dockerfile": "Dockerfile",
        "context": ".."
    },
    "remoteUser": "vscode",
    "postStartCommand": "uvicorn hello:app --reload",
    "forwardPorts": [
        8000
    ]
}